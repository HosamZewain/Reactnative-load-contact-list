/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Home from './resources/screens/Contacts';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Home);
